# 🤖 Lara Bot — WhatsApp IA via Neonize

## 🚀 Como rodar no Railway

1. Crie um projeto no [Railway.app](https://railway.app/).
2. Envie estes arquivos (`lara.py`, `requirements.txt`, `Procfile`).
3. Vá em **Settings → Variables** e adicione:

| Nome             | Valor (sua chave) |
|------------------|-------------------|
| `OPENAI_API_KEY` | sua chave da OpenAI |
| `NEWS_API_KEY`   | sua chave do NewsAPI |
| `HG_WEATHER_KEY` | sua chave do HG Weather (opcional) |

4. Clique em **Deploy**.
5. Veja os logs em tempo real para escanear o QR do WhatsApp.
6. Pronto! Lara estará online 24h 🎉

# bote-pronto
# bote-final
# bote-pronto
# lara-linda
# lara-linda
# lara-linda
